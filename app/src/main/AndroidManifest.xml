<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.hyochan.testapplication">

    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".clipboard_test.ClipboardTestActivity" />
        <activity android:name=".naver_test.NaverLoginTestActivity" />
        <activity android:name=".pinchzoom_test.PinchzoomTestActivity" />
        <activity android:name=".pinchzoom_test.ZoomImgActivity"/>
    </application>

</manifest>